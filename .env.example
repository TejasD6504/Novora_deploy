# Example environment file (DO NOT store secrets in the repo)
PORT=3000
SESSION_SECRET=novora_secret_REPLACE_ME

# Database (Postgres)
DB_TYPE=pg
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=root
DB_NAME=warehouse1

# Optional
NODE_ENV=development

# Admin user id (optional)
ADMIN_USER=admin
